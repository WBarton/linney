{
  "name": "linney",
  "description": "Linney website",
  "version": "0.0.0",
  "dependencies": {
    "bootstrap": "^4.5.0"
  },
  "scripts": {
    "sass:watch": "npx node-sass scss/main.scss Styles/main.css -w ./scss -r --include-path ./ --output-style compressed --source-map-embed",
    "sass": "npx node-sass scss/main.scss Styles/main.css --include-path ./ --output-style compressed --source-map-embed"
  },
  "author": "Linney Group",
  "license": "UNLICENSED",
  "private": true,
  "engines": {
    "node": ">=5.0"
  },
  "devDependencies": {
    "http-server": "^0.12.3",
    "node-sass": "^4.14.1"
  }
}
